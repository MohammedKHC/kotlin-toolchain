product: jvm/lib

settings:
  kotlin:
    version: <![Error] Secondary (`!!`) YAML type tags are not supported in Kotlin project files!>!!str<!> 2.20.0
    ksp:
      processorOptions:
        <![Error] Secondary (`!!`) YAML type tags are not supported in Kotlin project files!>!!int<!> foo: bar
        <![Error] Unexpected custom YAML type tag!>!unknown<!> baz: quu
        ? <![Error] Secondary (`!!`) YAML type tags are not supported in Kotlin project files!>!!str<!>
          <![Error] Unexpected compound key. Only string keys are supported!>[1, 2, 3]<!>
          : value
        <![Error] Expected a mapping key!>?
        : missing-key<!>

dependencies: <![Error] YAML anchors/aliases are not supported in Kotlin project files!>&anchor<!>
  - foo:bar:1

repositories: <![Error] Unexpected custom YAML type tag!>!tag<!>
  - <![Error] Secondary (`!!`) YAML type tags are not supported in Kotlin project files!>!!null<!> foo

dependencies@jvm: <![Error] YAML anchors/aliases are not supported in Kotlin project files!>*anchor<!>

<![Error] Multiple YAML documents are not supported!>---

settings@jvm:
  kotlin: [1,2,3]<!>